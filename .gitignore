# Runtime flight logs — contain player messages, session ids, IP addresses.
# NEVER commit these.
logs/
*.log
*.jsonl

# Server-side player sessions — story state + transcripts (per user/module). Player data; never commit.
sessions/

# Authoring studio working state — drafts are author scratch, versions are the on-box publish
# audit trail, studio-sessions are playtest scratch. None belong in the shared repo.
modules-draft/
modules-versions/
studio-sessions/

# 5090-specific firewall/ops cruft from the deploy gremlin hunt
fix-firewall.ps1

# Personal ops doc — LAN topology, SSH key paths, gateway internals. Kept out of the shared repo.
DEPLOY.md

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Local ops/handoff log — contains LAN topology (IPs, SSH paths). Kept out of the shared repo.
design/session-notes-*.md

# Local-only design notes — kept out of the shared repo.
design/ethics-and-safety.md
design/idea-true-crime-insertion.md

# Cross-channel story records (story-chat-id): state + transcripts + handoff history. Player data; never commit.
stories/
